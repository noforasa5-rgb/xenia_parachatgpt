import fs from 'node:fs';
import path from 'node:path';

if (!process.argv[2]) {
  throw new Error('Usage: node apply-achievement-xui-runtime-poc9.mjs XENIA_PATH');
}
const file = path.join(
    path.resolve(process.argv[2]),
    'src/xenia/ui/imgui_guest_notification.cc');
let data = fs.readFileSync(file, 'utf8').replaceAll('\r\n', '\n');
if (!data.includes(
        'XUI-POC8: original PNG renderer activated (skin geometry still provisional)')) {
  throw new Error('Expected the validated PoC 8 original-PNG renderer');
}
if (data.includes('XUI-POC9:')) {
  throw new Error('PoC 9 appears to be applied already');
}

const beginMarker = "        auto clamp01 = [](float v) {";
const endMarker = "        const auto& text_state = snapshot.text;";
const begin = data.indexOf(beginMarker);
const end = data.indexOf(endMarker, begin);
if (begin < 0 || end < 0 || data.indexOf(beginMarker, begin + 1) >= 0) {
  throw new Error('PoC 8 renderer replacement markers are missing or ambiguous');
}
data = data.slice(0, begin) + "        auto clamp01 = [](float v) {\n          return std::fmaxf(0.0f, std::fminf(1.0f, v));\n        };\n        auto rgba = [&](float r, float g, float b, float a) {\n          return ImGui::GetColorU32(ImVec4(r, g, b, clamp01(a)));\n        };\n        auto pos = [&](float x, float y) {\n          return ImVec2(origin.x + x * window_scale,\n                        origin.y + y * window_scale);\n        };\n\n        auto transformed_point = [&](const Xbox360XuiVisualState& state,\n                                     float x, float y, float pivot_x,\n                                     float pivot_y) {\n          return pos(state.position_x + pivot_x + (x - pivot_x) * state.scale_x,\n                     state.position_y + pivot_y + (y - pivot_y) * state.scale_y);\n        };\n\n        // The colors and stops below are the gradients stored in the supplied\n        // dashboard 17559 scr_Notification. Shade the vertices emitted by\n        // ImDrawList so rounded geometry retains per-vertex alpha and edge AA.\n        auto sample_xui_gradient = [&](const float* stop_positions,\n                                       const uint32_t* stop_colors,\n                                       uint32_t stop_count, float t,\n                                       float opacity, float coverage) {\n          t = clamp01(t);\n          uint32_t upper = 1;\n          while (upper < stop_count && t > stop_positions[upper]) {\n            ++upper;\n          }\n          if (upper >= stop_count) upper = stop_count - 1;\n          const uint32_t lower = upper ? upper - 1 : 0;\n          const float span = stop_positions[upper] - stop_positions[lower];\n          const float local_t =\n              span > 0.000001f ? clamp01((t - stop_positions[lower]) / span)\n                               : 0.0f;\n          const uint32_t c0 = stop_colors[lower];\n          const uint32_t c1 = stop_colors[upper];\n          auto component = [&](uint32_t shift) {\n            const float v0 = float((c0 >> shift) & 0xFF) / 255.0f;\n            const float v1 = float((c1 >> shift) & 0xFF) / 255.0f;\n            return v0 + (v1 - v0) * local_t;\n          };\n          return rgba(component(16), component(8), component(0),\n                      component(24) * opacity * coverage);\n        };\n        auto shade_vertical_gradient = [&](int first_vertex, const ImVec2& a,\n                                           const ImVec2& b,\n                                           const float* stop_positions,\n                                           const uint32_t* stop_colors,\n                                           uint32_t stop_count,\n                                           float opacity) {\n          const float height = std::fmaxf(0.001f, b.y - a.y);\n          for (int i = first_vertex; i < draw->VtxBuffer.Size; ++i) {\n            ImDrawVert& vertex = draw->VtxBuffer[i];\n            const float coverage =\n                float((vertex.col >> IM_COL32_A_SHIFT) & 0xFF) / 255.0f;\n            vertex.col = sample_xui_gradient(\n                stop_positions, stop_colors, stop_count,\n                (vertex.pos.y - a.y) / height, opacity, coverage);\n          }\n        };\n        auto draw_radial_gradient = [&](const ImVec2& center, float radius,\n                                        const float* stop_positions,\n                                        const uint32_t* stop_colors,\n                                        uint32_t stop_count, float opacity) {\n          if (radius <= 0.001f || opacity <= 0.001f) return;\n          const int first_vertex = draw->VtxBuffer.Size;\n          draw->AddCircleFilled(center, radius, IM_COL32_WHITE, 64);\n          for (int i = first_vertex; i < draw->VtxBuffer.Size; ++i) {\n            ImDrawVert& vertex = draw->VtxBuffer[i];\n            const float dx = vertex.pos.x - center.x;\n            const float dy = vertex.pos.y - center.y;\n            const float coverage =\n                float((vertex.col >> IM_COL32_A_SHIFT) & 0xFF) / 255.0f;\n            vertex.col = sample_xui_gradient(\n                stop_positions, stop_colors, stop_count,\n                float(std::sqrt(dx * dx + dy * dy)) / radius, opacity,\n                coverage);\n          }\n        };\n\n        static constexpr float kCurveStops[] = {\n            0.019000f, 0.085000f, 0.125000f, 0.156000f,\n            0.832000f, 0.880000f, 0.928000f, 0.976000f};\n        static constexpr uint32_t kCurveColors[] = {\n            0x00EBEBEB, 0x32EBEBEB, 0xFF323232, 0xFF3C3C3C,\n            0xFF3C3C3C, 0xFF323232, 0x32EBEBEB, 0x00EBEBEB};\n        static constexpr float kRoundStops[] = {0.576000f, 0.681000f,\n                                                0.755000f, 0.866000f};\n        static constexpr uint32_t kRoundColors[] = {\n            0xFF3C3C3C, 0xFF323232, 0x36EBEBEB, 0x00EBEBEB};\n        static constexpr float kLogoBackStops[] = {0.800000f, 0.913726f};\n        static constexpr uint32_t kLogoBackColors[] = {0xFF0F0F0F,\n                                                       0x000F0F0F};\n        static constexpr float kBgStops[] = {0.674510f, 0.756863f,\n                                             0.913726f, 0.949020f};\n        static constexpr uint32_t kBgColors[] = {\n            0x00464646, 0xFF646464, 0xFF828282, 0x00969696};\n        static constexpr float kExplosionStops[] = {\n            0.619608f, 0.737255f, 0.831373f, 0.949020f};\n        static constexpr uint32_t kExplosionColors[] = {\n            0xFF0F0F0F, 0x960FEB0F, 0x6482EB0F, 0x000FEB0F};\n\n        const auto& curve = snapshot.bg_curve;\n        if (curve.show && curve.opacity > 0.001f) {\n          const ImVec2 p0 = transformed_point(curve, 0.0f, 0.0f,\n                                               32.559700f, 19.259399f);\n          const ImVec2 p1 = transformed_point(curve, 356.0f, 39.0f,\n                                               32.559700f, 19.259399f);\n          const ImVec2 a(std::fminf(p0.x, p1.x), std::fminf(p0.y, p1.y));\n          const ImVec2 b(std::fmaxf(p0.x, p1.x), std::fmaxf(p0.y, p1.y));\n          const float rounding =\n              std::fminf((b.x - a.x) * 0.5f, (b.y - a.y) * 0.5f);\n          const int first_vertex = draw->VtxBuffer.Size;\n          draw->AddRectFilled(a, b, IM_COL32_WHITE, rounding);\n          shade_vertical_gradient(first_vertex, a, b, kCurveStops,\n                                  kCurveColors, 8, curve.opacity);\n        }\n\n        // The left cap is a separate radial figure in the original scene.\n        const ImVec2 logo_center = pos(32.768760f, 29.204125f);\n        const auto& round = snapshot.round;\n        if (round.show && round.opacity > 0.001f) {\n          draw_radial_gradient(logo_center, 30.65f * window_scale,\n                               kRoundStops, kRoundColors, 4, round.opacity);\n        }\n\n        const auto& bg = snapshot.bg;\n        if (bg.show && bg.opacity > 0.001f) {\n          draw_radial_gradient(\n              transformed_point(bg, 38.222198f, 39.111099f, 38.222198f,\n                                39.111099f),\n              38.0f * float(std::fabs(bg.scale_x)) * window_scale, kBgStops,\n              kBgColors, 4, bg.opacity);\n        }\n        const auto& back = snapshot.logo_back;\n        if (back.show && back.opacity > 0.001f) {\n          draw_radial_gradient(\n              transformed_point(back, 23.0f, 23.0f, 23.0f, 23.0f),\n              23.0f * float(std::fabs(back.scale_x)) * window_scale,\n              kLogoBackStops, kLogoBackColors, 2, back.opacity);\n        }\n\n        const auto draw_indicator = [&](const Xbox360XuiVisualState& state,\n                                        float base_x, float base_y,\n                                        float angle_min, float angle_max) {\n          if (!state.show || state.opacity <= 0.001f) return;\n          const ImVec2 center =\n              pos(32.768760f + (state.position_x - base_x) * 0.25f,\n                  29.204125f + (state.position_y - base_y) * 0.25f);\n          const float radius = 37.5f * window_scale;\n          draw->PathArcTo(center, radius, angle_min, angle_max, 16);\n          draw->PathStroke(rgba(0.55f, 0.91f, 0.12f,\n                                0.28f * state.opacity),\n                           0, 4.5f * window_scale);\n          draw->PathArcTo(center, radius, angle_min, angle_max, 16);\n          draw->PathStroke(rgba(0.78f, 0.92f, 0.39f,\n                                0.92f * state.opacity),\n                           0, 1.25f * window_scale);\n        };\n\n        const auto& explosion = snapshot.explosion;\n        if (explosion.show && explosion.opacity > 0.001f) {\n          draw_radial_gradient(\n              transformed_point(explosion, 22.5f, 22.5f,\n                                 22.444500f, 23.629700f),\n              31.0f * float(std::fabs(explosion.scale_x)) * window_scale,\n              kExplosionStops, kExplosionColors, 4, explosion.opacity);\n        }\n\n        constexpr float kPi = 3.14159265358979323846f;\n        draw_indicator(snapshot.indicator1, -44.0f, -48.0f,\n                       -0.94f * kPi, -0.56f * kPi);\n        draw_indicator(snapshot.indicator2, 110.0f, -48.0f,\n                       -0.44f * kPi, -0.06f * kPi);\n        draw_indicator(snapshot.indicator3, -44.0f, 107.0f,\n                       0.56f * kPi, 0.94f * kPi);\n        draw_indicator(snapshot.indicator4, 110.0f, 107.0f,\n                       0.06f * kPi, 0.44f * kPi);\n\n        // Both supplied image elements use SizeMode=16. The PNG remains at\n        // native pixel size, centered on the presenter, and the presenter\n        // bounds clip its transparent margins. This matches the visible\n        // content dimensions observed in the original 108x108 and 32x32 PNGs.\n        auto draw_original_image_size_mode_16 = [&]\n            (const Xbox360XuiVisualState& state, ImmediateTexture* texture,\n             float control_width, float control_height, float pivot_x,\n             float pivot_y) {\n          if (!state.show || state.opacity <= 0.001f) return;\n          const ImVec2 p0 = transformed_point(state, 0.0f, 0.0f, pivot_x,\n                                               pivot_y);\n          const ImVec2 p1 = transformed_point(state, control_width,\n                                               control_height, pivot_x,\n                                               pivot_y);\n          const ImVec2 clip_min(std::fminf(p0.x, p1.x),\n                                std::fminf(p0.y, p1.y));\n          const ImVec2 clip_max(std::fmaxf(p0.x, p1.x),\n                                std::fmaxf(p0.y, p1.y));\n          const ImVec2 center = pos(state.position_x + pivot_x,\n                                    state.position_y + pivot_y);\n          const float half_width =\n              float(texture->width) * float(std::fabs(state.scale_x)) *\n              window_scale * 0.5f;\n          const float half_height =\n              float(texture->height) * float(std::fabs(state.scale_y)) *\n              window_scale * 0.5f;\n          draw->PushClipRect(clip_min, clip_max, true);\n          draw->AddImage(reinterpret_cast<ImTextureID>(texture),\n                         ImVec2(center.x - half_width, center.y - half_height),\n                         ImVec2(center.x + half_width, center.y + half_height),\n                         ImVec2(0, 0), ImVec2(1, 1),\n                         rgba(1, 1, 1, state.opacity));\n          draw->PopClipRect();\n        };\n        draw_original_image_size_mode_16(snapshot.xenon_logo, original_logo,\n                                         58.0f, 58.0f, 29.0f, 29.0f);\n        draw_original_image_size_mode_16(snapshot.image, original_achievement,\n                                         24.0f, 24.0f, 12.0f, 12.0f);\n\n" + data.slice(end);
data = data.replace(
    'XUI-POC8: original PNG renderer activated (skin geometry still provisional)',
    'XUI-POC9: original gradient renderer activated (SizeMode16 native-center/clipped)');
data = data.replaceAll('XUI-POC8:', 'XUI-POC9:');
fs.writeFileSync(file, data);
console.log('Applied PoC 9 dashboard-17559 gradients, radial figures, arcs and SizeMode16 image layout.');
